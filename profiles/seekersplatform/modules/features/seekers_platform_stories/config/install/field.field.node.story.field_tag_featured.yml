langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_tag_featured
    - node.type.story
    - taxonomy.vocabulary.featured
id: node.story.field_tag_featured
field_name: field_tag_featured
entity_type: node
bundle: story
label: 'Featured On?'
description: 'Please indicate if this Story should be Featured on an area of the site (e.g., homepage header/cards, stories header/cards, destinations head/cards, etc.)'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      featured: featured
    sort:
      field: _none
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
